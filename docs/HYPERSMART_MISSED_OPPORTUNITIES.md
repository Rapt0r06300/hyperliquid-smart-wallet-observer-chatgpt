# HyperSmart Missed Opportunities

Missed opportunities explain why the bot did not simulate something.

Implemented:

- `src/hl_observer/scanner/opportunity_detector.py`
- `src/hl_observer/scanner/missed_opportunity_logger.py`
- `python -m hl_observer scanner-priority-report --network-read --max-leaders 3`
- `python -m hl_observer missed-opportunities --period 24h`

The report is research-only and never a signal.

